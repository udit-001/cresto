.lific.json

# Playwright/MCP browser test artifacts
.playwright-mcp/

# Development screenshots
*.png
*.jpg
*.jpeg

# Sample/temp data files
payslip-*.pdf
payslip_*.txt
payslips-filter-badges
*_AIS_*.json
*_AIS_*_decrypted.json
taxpnl-*.xlsx
tax_page_prototype.html
AIS Utility_*.zip
ITDe-Filing-*.zip

# Income Tax portal downloads (AIS, ITR, challan receipts, verification zips)
ITR-*.json
ITR-*.zip
ITR2_*.json
ITR2_*.zip
*_ChallanReceipt.pdf
[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9].json
*[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9].json
BLUPM*-*.zip

# Tailwind CSS standalone binary (downloaded via `go run . tailwind download`)
.bin/

# Compiled CSS — regenerated from web/input.css via `go run . tailwind build`.
# Committed so `go build` works without the Tailwind CLI present; the rule
# below keeps local rebuilds from creating noisy diffs in other CSS files.
internal/web/static/app.css

# Local SQLite database + uploaded PDFs (created at runtime by `serve`)
*.db
*.db-shm
*.db-wal
data/
payslips/

# Secrets (AMO credentials for extension signing)
.env

# web-ext build artifacts (intermediate, not committed)
web-ext-artifacts/

# Build artifacts
/bin/
